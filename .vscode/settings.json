// .vscode/settings.json
{
    // aponta o Pylance para buscar módulos dentro da pasta "eyeware-sdk" no seu workspace
    "python.analysis.extraPaths": [
        "${workspaceFolder}/eyeware-sdk"
    ]
}